# Ftune_Results_Manipulation
After the fine-tuning of models, to analyse where models improve and where models are failing or learning something wrong, we will manipulate the results and keep track here.
